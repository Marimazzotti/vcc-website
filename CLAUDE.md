# Instructions for Claude Code

## Branches
- Never push directly to `main`
- `main` is the production branch — only humans merge into it via Pull Request
- Only push to `staging` when I explicitly say "push to staging" or "deploy to staging"

## Default behaviour
- By default, make changes locally only — do NOT push to GitHub automatically
- Wait for my instruction before pushing anything to `staging`